interface:
  display_name: "Refine Prompts"
  short_description: "Turn rough requests into reliable prompts with clear unknowns"
  default_prompt: "Use $refine-prompts to uncover the need, expose material unknowns, and return one optimized prompt:"

policy:
  allow_implicit_invocation: false
