interface:
  display_name: "Create Better Spritesheet"
  short_description: "Build and verify evidence-admitted sprite packages"
  default_prompt: "Use $create-better-spritesheet to prepare and machine-admit canonical candidates before approval, bind motion gates to the admission proof, build spritesheet-package/v3 manifest.json, and replay canonical admission and every target cell."
