interface:
  display_name: "Create Better Spritesheet"
  short_description: "Create and verify sprite-sheet packages"
  default_prompt: "Use $create-better-spritesheet to create, rebuild, diagnose, review, or integrate these sprite-sheet artifacts."
