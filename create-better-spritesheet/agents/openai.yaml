interface:
  display_name: "Create Better Spritesheet"
  short_description: "Create and verify v2 production spritesheet packages"
  default_prompt: "Use $create-better-spritesheet to author and approve fixed-canvas canonical references, generate hash-approved high-resolution keyframes and in-betweens, build one spritesheet-package/v2 with deterministic target-cell rendering, and verify every cell by replaying the sampler."
