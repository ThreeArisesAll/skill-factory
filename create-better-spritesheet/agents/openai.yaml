interface:
  display_name: "Create Better Spritesheet"
  short_description: "Create and validate multi-action 2D character spritesheets"
  default_prompt: "Use $create-better-spritesheet to honor action-reference opt-outs, reuse resolved outline rules without reconfirming, apply any outline to the fixed-size high-resolution pre-master before locking the canonical master, and create a contract-matched spritesheet."
